pipeline {
  agent any

  stages {
    stage('Moved') {
      steps {
        error '''
developer_build has moved to the GitOps repository.

Use:
  yas-gitops/jenkins/Jenkinsfile.developer_build

This keeps the application repository CI-only, like the reference setup.
'''
      }
    }
  }
}
